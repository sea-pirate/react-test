npm i react react-dom # if no cdn


@babel/preset-react
- includes: @babel/plugin-transform-react-jsx
- includes: @babel/plugin-transform-react-display-name
- includes: @babel/plugin-syntax-jsx

@babel/core --save-dev
@babel/cli --save-dev
npx babel
- npx babel script.js --out-file script-compiled.js
- npx babel script.js --watch --out-file script-compiled.js
- npx babel --watch ./src --out-dir ./public # --presets react-app/prod


babel-preset-minify
- "presets": ["minify"]

/*
.babelrc.js
module.exports = {
  presets: [
    [
      "@babel/preset-react",
      {
        development: process.env.BABEL_ENV === "development",
      },
    ],
  ],
};
*/

// webpack will auto minify all dependencies into one file
npm install --save-dev webpack webpack-dev-server webpack-cli
npm i --save-dev babel-loader

/*webpack.config.js
module.exports = {
  entry: './src/index.js',
  output: {
    path: __dirname + '/dist',
    publicPath: '/',
    filename: 'bundle.js'
  },
  devServer: {
    contentBase: './dist',
  },
  module: {
    rules: [
    {
      test: /\.(js|jsx)$/,
      exclude: /node_modules/,
      use: ['babel-loader']
    }
    ]
  },
};
*/